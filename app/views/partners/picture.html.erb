<div id="hp_intro">
	<%= @page_title %>
</div>
<p>	
<% if @partner.has_picture? %>
	<img src="/pictures/get_450/<%= @partner.picture_id %>">
<% end %>

<% form_for(:partner, :url => {:action => "picture_save" }, :html => {:multipart => true}) do |f| %>
<%= f.error_messages %>

<% fields_for :picture, @partner.picture do |p| %>
<p>
  <%= p.file_field :picture %><br/>
  <span class="small"><%= t('pictures.must') %></span>
<% end %>

<p><%= submit_tag t('pictures.submit'), :class => "yellow_button" %></p>
<% end %>
