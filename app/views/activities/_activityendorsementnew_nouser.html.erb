<img src="/images/icons/accept.png" height="16" width="16" border="0" class="icon">
<% if activity.has_ad? %>
	<% if activity.endorsement and activity.endorsement.attribute_present?("position") %>
		<%= t('activity.endorsement.new.ad.nouser.known', :user_name => link_to(activity.user.login, activity.user), :priority_name => link_to(activity.priority.name, activity.priority), :position => activity.endorsement.position, :ad_url => priority_ad_path(activity.priority,activity.ad), :ad_user =>  activity.ad.user.name.possessive) %>
	<% elsif activity.priority %>
		<%= t('activity.endorsement.new.ad.nouser.unknown', :user_name => link_to(activity.user.login, activity.user), :priority_name => link_to(activity.priority.name, activity.priority), :ad_url => priority_ad_path(activity.priority,activity.ad), :ad_user =>  activity.ad.user.name.possessive) %>
	<% end %>
<% else %>
	<% if activity.endorsement and activity.endorsement.attribute_present?("position") %>
		<%= t('activity.endorsement.new.nouser.known', :user_name => link_to(activity.user.login, activity.user), :priority_name => link_to(activity.priority.name, activity.priority), :position => activity.endorsement.position) %>
	<% elsif activity.priority %>
		<%= t('activity.endorsement.new.nouser.unknown', :user_name => link_to(activity.user.login, activity.user), :priority_name => link_to(activity.priority.name, activity.priority)) %>
	<% end %>
<% end %>