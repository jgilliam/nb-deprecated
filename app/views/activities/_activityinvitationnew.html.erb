<img src="/images/icons/email.png" height="16" width="16" border="0" class="icon">
<% if activity.user %>
	<%= t('activity.invitation.new.name', :user_name => link_to(activity.user.login, activity.user), :government_name => current_government.name)%>
<% else %>
	<%= t('activity.invitation.new.name', :user_name => "Someone", :government_name => current_government.name)%>
<% end %>
