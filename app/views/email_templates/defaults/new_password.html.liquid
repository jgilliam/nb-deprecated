Hello {{ user.name }},

Here's your new temporary {{ government.name }} password: {{ new_password }}

{{ user.root_url }}

Once you login, you can change this password to something more memorable here:

{{ user.root_url }}passwords/{{ user.id }}/edit

And if you forget again, it's not a big deal, you can have a new one sent to you here:

{{ user.root_url }}passwords/new