Research requested by <%= research_task_requester(@research_task) %>:
<div id="hp_intro"><%= @page_title %></div>
<p>
<table border="0" cellspacing="0" cellpadding="0" width="100%">
	<tr>
		<td valign="top">
			Use this document to collate all the research needed. 
			<% if @research_task.attribute_present?("content") %>Details from the requester have been added automatically for easy reference. <% end %>
			Anyone will be able to add to it and edit it.  We'll keep a record of revisions, just like Wikipedia.
			
			<p>
			<% remote_form_for :document, :url => document_save_research_task_url(@research_task)  do |f| %>
			<p>
			Title <span class="small">60 characters</span><br />
			<%= f.text_field :name, :size => 50, :maxlength => 60 %>		
			</p>
			<p>
			<%= f.text_area :content, :rows => 20, :cols => 60 %>
			</p>

			<p>
			<table border="0" cellspacing="0" cellpading="0">
				<tr><td valign="top">
					<%= f.submit "Add document", :id => "submit", :class => "yellow_button" %>
				</td><td valign="top" style="padding-left: 10px; padding-top: 3px;">
					<div id="errors" class="error"><%= f.error_messages %></div>
				</td>
				</tr>
			</table>
			</p>

			<% end %>
			<p>
			<%= blurb('textile')%>
			
			<% if @research_task.attribute_present?("content") %>
			<p>
			<div class="heading">The Research Request</div>
			<a name="req"></a>
			<%= render :partial => "research_tasks/show", :locals => {:task => @research_task}%>
			<% end %>

		</td>	

		<td valign="top" width="160" style="padding-left: 25px;">

		</td>	

	</tr>
</table>


